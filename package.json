{
  "name": "begin-ts",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^4.5.4"
  }
}
